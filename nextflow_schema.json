{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/pinellolab/CRISPR_Pipeline/main/nextflow_schema.json",
    "title": "CRISPR Pipeline parameters",
    "description": "Pipeline parameters for single-cell Perturb-seq CRISPR screen processing, guide assignment, inference, and optional evaluation outputs.",
    "type": "object",
    "additionalProperties": true,
    "definitions": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "description": "Define the input samplesheet and output directory.",
            "required": [
                "input"
            ],
            "properties": {
                "input": {
                    "type": [
                        "string",
                        "null"
                    ],
                    "format": "file-path",
                    "mimetype": "text/csv",
                    "pattern": "^\\S+\\.(csv|tsv)$",
                    "description": "Path to the pipeline samplesheet.",
                    "help_text": "The samplesheet provides modality-specific FASTQ paths and metadata used by the pipeline, including R1_path, R2_path, file_modality, measurement_sets, seqspec, barcode_onlist, guide_design, and barcode_hashtag_map when hashing is enabled."
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "default": "./pipeline_outputs",
                    "description": "Directory where final pipeline outputs are published."
                }
            }
        },
        "assay_and_library_options": {
            "title": "Assay and library options",
            "type": "object",
            "fa_icon": "fas fa-dna",
            "description": "Options describing the assay design, sequencing chemistry, and barcode handling.",
            "properties": {
                "ENABLE_DATA_HASHING": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable the hashing branch of the workflow.",
                    "help_text": "When true, the workflow expects hash modality rows in the samplesheet, runs hashing mapping, filtering, demultiplexing, and uses the hashing dashboard path."
                },
                "ENABLE_SCRUBLET": {
                    "type": "boolean",
                    "default": false,
                    "description": "Run Scrublet doublet detection before guide assignment in non-hashing workflows."
                },
                "is_10x3v3": {
                    "type": "boolean",
                    "default": true,
                    "description": "Use fixed 10x Genomics 3' v3 guide chemistry handling.",
                    "help_text": "When true, guide mapping uses the 10XV3/kite:10xFB chemistry path instead of deriving guide chemistry entirely from the guide seqspec."
                },
                "reverse_complement_guides": {
                    "type": "boolean",
                    "default": false,
                    "description": "Reverse-complement guide spacer sequences while building the guide reference."
                },
                "spacer_tag": {
                    "type": [
                        "string",
                        "null"
                    ],
                    "default": "GAGTACATGGGG",
                    "description": "Fixed sequence immediately upstream of the guide spacer.",
                    "help_text": "When provided, guide mapping keeps the guide read from the seqspec but resets the guide feature coordinates so the mapper can search the whole guide read around this spacer tag. Use null or an empty value to rely on the seqspec feature coordinates."
                },
                "scrna_workflow": {
                    "type": "string",
                    "default": "standard",
                    "enum": [
                        "standard",
                        "nac"
                    ],
                    "description": "kb count workflow used for scRNA mapping.",
                    "help_text": "standard runs regular transcriptome counting. nac runs the nascent-aware workflow with cDNA and nascent references."
                },
                "use_multimapping": {
                    "type": "boolean",
                    "default": false,
                    "description": "Pass kb count multimapping mode to scRNA mapping and preserve that setting during AnnData concatenation."
                },
                "replace_barcodes": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable barcode replacement during RNA and guide mapping.",
                    "help_text": "When true, the mapping modules pass bc_replacement_file to kb count and downstream concatenation expects the modified barcode outputs."
                },
                "bc_replacement_file": {
                    "type": "string",
                    "format": "file-path",
                    "default": "",
                    "description": "Barcode replacement table used when replace_barcodes is true."
                },
                "DUAL_GUIDE": {
                    "type": "boolean",
                    "default": false,
                    "description": "Enable dual-guide-aware aggregation when concatenating guide-assigned MuData."
                },
                "Multiplicity_of_infection": {
                    "type": "string",
                    "default": "high",
                    "enum": [
                        "high",
                        "low"
                    ],
                    "description": "Expected CRISPR screen multiplicity of infection.",
                    "help_text": "Stored in the MuData object and used by downstream guide-assignment/inference context."
                }
            }
        },
        "reference_options": {
            "title": "Reference options",
            "type": "object",
            "fa_icon": "fas fa-book",
            "description": "Reference transcriptome and annotation inputs.",
            "properties": {
                "use_igvf_reference": {
                    "type": "boolean",
                    "default": true,
                    "description": "Use the prebuilt IGVF transcriptome reference for RNA mapping.",
                    "help_text": "When true, the RNA mapping reference is taken from the IGVF reference path and REFERENCE_transcriptome is ignored for that mapping step."
                },
                "REFERENCE_transcriptome": {
                    "type": "string",
                    "default": "human",
                    "description": "Transcriptome name passed to kb-python reference download when not using the prebuilt IGVF reference."
                },
                "REFERENCE_gtf_download_path": {
                    "type": "string",
                    "format": "uri",
                    "default": "https://ftp.ebi.ac.uk/pub/databases/gencode/Gencode_human/release_43/gencode.v43.annotation.gtf.gz",
                    "description": "URL for the GTF annotation downloaded when REFERENCE_gtf_local_path does not exist."
                },
                "REFERENCE_gtf_local_path": {
                    "type": "string",
                    "format": "file-path",
                    "default": "/path/to/gencode_gtf.gtf.gz",
                    "description": "Local GTF annotation path.",
                    "help_text": "If this file exists at runtime, preprocessing and inference use it instead of downloading REFERENCE_gtf_download_path."
                }
            }
        },
        "quality_control_options": {
            "title": "Quality control options",
            "type": "object",
            "fa_icon": "fas fa-filter",
            "description": "Cell, gene, and barcode filtering options used during preprocessing and QC reporting.",
            "properties": {
                "QC_min_genes_per_cell": {
                    "type": "integer",
                    "default": 800,
                    "minimum": 0,
                    "description": "Minimum detected genes required to keep a cell when QC_barcode_filter is none."
                },
                "QC_min_cells_per_gene": {
                    "type": "number",
                    "default": 0.05,
                    "minimum": 0,
                    "description": "Minimum cell support required to keep a gene.",
                    "help_text": "Passed to preprocessing and to MuData guide-assignment aggregation. Values are typically interpreted as a fraction of cells."
                },
                "QC_pct_mito": {
                    "type": "number",
                    "default": 15,
                    "minimum": 0,
                    "maximum": 100,
                    "description": "Maximum mitochondrial read percentage allowed per cell."
                },
                "QC_batch_col": {
                    "type": "string",
                    "default": "batch",
                    "description": "Observation metadata column used as the batch label in additional QC plots."
                },
                "QC_barcode_filter": {
                    "type": "string",
                    "default": "knee2",
                    "enum": [
                        "none",
                        "knee",
                        "knee2"
                    ],
                    "description": "RNA barcode filtering strategy.",
                    "help_text": "none skips the RNA UMI knee filter and uses QC_min_genes_per_cell. knee uses the first knee point from the barcode rank curve. knee2 uses the second, stricter knee point."
                }
            }
        },
        "guide_assignment_options": {
            "title": "Guide assignment options",
            "type": "object",
            "fa_icon": "fas fa-barcode",
            "description": "Guide assignment method and thresholds.",
            "properties": {
                "GUIDE_ASSIGNMENT_method": {
                    "type": "string",
                    "default": "sceptre",
                    "enum": [
                        "sceptre",
                        "cleanser"
                    ],
                    "description": "Method used to assign guide RNAs to cells."
                },
                "GUIDE_ASSIGNMENT_capture_method": {
                    "type": "string",
                    "default": "CROP-seq",
                    "description": "Guide capture method recorded in MuData and passed to Cleanser when GUIDE_ASSIGNMENT_method is cleanser."
                },
                "GUIDE_ASSIGNMENT_cleanser_probability_threshold": {
                    "type": "number",
                    "default": 1,
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Probability threshold passed to Cleanser guide assignment."
                },
                "GUIDE_ASSIGNMENT_SCEPTRE_probability_threshold": {
                    "type": "number",
                    "default": 0.8,
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Posterior probability threshold for SCEPTRE mixture-based guide assignment."
                },
                "GUIDE_ASSIGNMENT_SCEPTRE_n_em_rep": {
                    "type": "integer",
                    "default": 5,
                    "minimum": 1,
                    "description": "Number of EM initializations passed to SCEPTRE guide assignment."
                }
            }
        },
        "inference_options": {
            "title": "Inference options",
            "type": "object",
            "fa_icon": "fas fa-chart-line",
            "description": "Differential expression and perturbation inference settings.",
            "properties": {
                "INFERENCE_method": {
                    "type": "string",
                    "default": "default",
                    "enum": [
                        "default",
                        "sceptre",
                        "perturbo",
                        "sceptre,perturbo"
                    ],
                    "description": "Inference method to run.",
                    "help_text": "default runs cis SCEPTRE, cis PerTurbo, trans PerTurbo, and writes the merged cis/trans output tables. sceptre runs SCEPTRE only. perturbo runs PerTurbo only. sceptre,perturbo runs both methods on the same cis pair set."
                },
                "INFERENCE_input_mudata": {
                    "type": [
                        "string",
                        "null"
                    ],
                    "format": "file-path",
                    "default": null,
                    "description": "Input MuData file for -entry INFERENCE_FROM_MUDATA runs."
                },
                "INFERENCE_target_guide_pairing_strategy": {
                    "type": "string",
                    "default": "default",
                    "enum": [
                        "default",
                        "by_distance",
                        "predefined_pairs"
                    ],
                    "description": "Strategy used to define guide-target pairs before inference.",
                    "help_text": "default builds cis pairs using intended targets and the configured max distance. by_distance builds distance-based cis pairs. predefined_pairs uses INFERENCE_predefined_pairs_to_test."
                },
                "INFERENCE_predefined_pairs_to_test": {
                    "type": [
                        "string",
                        "null"
                    ],
                    "format": "file-path",
                    "default": null,
                    "description": "CSV file containing predefined guide-target pairs.",
                    "help_text": "Required when INFERENCE_target_guide_pairing_strategy is predefined_pairs."
                },
                "INFERENCE_max_target_distance_bp": {
                    "type": "integer",
                    "default": 1000000,
                    "minimum": 0,
                    "description": "Maximum genomic distance, in base pairs, used when constructing cis guide-target pairs."
                },
                "INFERENCE_PERTURBO_BATCH_SIZE": {
                    "type": "integer",
                    "default": 4096,
                    "minimum": 1,
                    "description": "Batch size passed to PerTurbo inference for cis and trans runs."
                },
                "INFERENCE_PERTURBO_TRANS_MAX_GENES_PER_CHUNK": {
                    "type": "integer",
                    "default": 8000,
                    "description": "Maximum genes per chunk for trans PerTurbo all-by-all inference.",
                    "help_text": "Values greater than 0 cap each balanced trans gene chunk. Values less than or equal to 0 disable trans gene chunking in the PerTurbo chunked runner."
                },
                "INFERENCE_SCEPTRE_side": {
                    "type": "string",
                    "default": "both",
                    "enum": [
                        "both",
                        "left",
                        "right"
                    ],
                    "description": "Alternative-hypothesis side passed to SCEPTRE inference."
                },
                "INFERENCE_SCEPTRE_grna_integration_strategy": {
                    "type": "string",
                    "default": "union",
                    "description": "Guide RNA integration strategy passed to SCEPTRE inference."
                },
                "INFERENCE_SCEPTRE_resampling_approximation": {
                    "type": "string",
                    "default": "skew_normal",
                    "description": "Resampling approximation passed to SCEPTRE inference."
                },
                "INFERENCE_SCEPTRE_control_group": {
                    "type": "string",
                    "default": "complement",
                    "description": "Control group strategy passed to SCEPTRE inference."
                },
                "INFERENCE_SCEPTRE_resampling_mechanism": {
                    "type": "string",
                    "default": "default",
                    "description": "Resampling mechanism passed to SCEPTRE inference."
                },
                "INFERENCE_SCEPTRE_CHUNK_MODE": {
                    "type": "string",
                    "default": "auto",
                    "enum": [
                        "auto",
                        "off",
                        "force"
                    ],
                    "description": "SCEPTRE MuData gene-chunking mode before inference.",
                    "help_text": "auto chunks when the cell-by-gene matrix exceeds INFERENCE_SCEPTRE_MAX_MATRIX_ENTRIES. off keeps a single input. force chunks regardless of matrix size."
                },
                "INFERENCE_SCEPTRE_MAX_MATRIX_ENTRIES": {
                    "type": "integer",
                    "default": 2147483647,
                    "minimum": 1,
                    "description": "Matrix-size threshold used by SCEPTRE auto chunking."
                },
                "INFERENCE_SCEPTRE_GENE_CHUNK_SIZE": {
                    "type": "integer",
                    "default": 1000,
                    "minimum": 1,
                    "description": "Number of genes per SCEPTRE chunk when chunking is enabled."
                },
                "INFERENCE_SCEPTRE_FORCE_CHUNK": {
                    "type": "boolean",
                    "default": false,
                    "description": "Force SCEPTRE chunking regardless of matrix size.",
                    "help_text": "This is a compatibility flag that passes --force-chunk to chunk_mudata_sceptre.py."
                }
            }
        },
        "benchmark_options": {
            "title": "Benchmark options",
            "type": "object",
            "fa_icon": "fas fa-vial",
            "description": "Optional transcription-factor benchmark analysis against ENCODE BED files.",
            "properties": {
                "ENABLE_BENCHMARK": {
                    "type": "boolean",
                    "default": false,
                    "description": "Run the transcription factor benchmark workflow after inference."
                },
                "ENCODE_BED_DIR": {
                    "type": "string",
                    "format": "directory-path",
                    "default": "${projectDir}/encode_bed_files",
                    "description": "Directory containing ENCODE BED files used by the optional benchmark workflow."
                }
            }
        },
        "network_options": {
            "title": "Network plot options",
            "type": "object",
            "fa_icon": "fas fa-project-diagram",
            "description": "Network-display settings used by dashboard/evaluation plots.",
            "properties": {
                "NETWORK_custom_central_nodes": {
                    "type": "string",
                    "default": "undefined",
                    "description": "Comma-separated custom central nodes to highlight in network plots."
                },
                "NETWORK_central_nodes_num": {
                    "type": "integer",
                    "default": 1,
                    "minimum": 0,
                    "description": "Number of central nodes to highlight when NETWORK_custom_central_nodes is undefined."
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/input_output_options"
        },
        {
            "$ref": "#/definitions/assay_and_library_options"
        },
        {
            "$ref": "#/definitions/reference_options"
        },
        {
            "$ref": "#/definitions/quality_control_options"
        },
        {
            "$ref": "#/definitions/guide_assignment_options"
        },
        {
            "$ref": "#/definitions/inference_options"
        },
        {
            "$ref": "#/definitions/benchmark_options"
        },
        {
            "$ref": "#/definitions/network_options"
        }
    ]
}
